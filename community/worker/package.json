{
  "name": "mbc-community",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mbc-community --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute mbc-community --local --file=./schema.sql"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
